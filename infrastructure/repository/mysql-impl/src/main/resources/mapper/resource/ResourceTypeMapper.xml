<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.catface996.aiops.repository.mysql.mapper.resource.ResourceTypeMapper">

    <!-- 根据类型编码查询资源类型 -->
    <select id="selectByCode" resultType="com.catface996.aiops.repository.mysql.po.resource.ResourceTypePO">
        SELECT id, code, name, description, icon, is_system, created_at, updated_at
        FROM resource_type
        WHERE code = #{code}
        LIMIT 1
    </select>

    <!-- 查询所有系统预置的资源类型 -->
    <select id="selectSystemTypes" resultType="com.catface996.aiops.repository.mysql.po.resource.ResourceTypePO">
        SELECT id, code, name, description, icon, is_system, created_at, updated_at
        FROM resource_type
        WHERE is_system = 1
        ORDER BY id ASC
    </select>

</mapper>
